# Design: any-proxy Rust 公网匿名 CORS Relay

Generated by /office-hours on 2026-07-13
Branch: unknown
Repo: local/any-proxy
Status: APPROVED
Mode: Builder

## Problem Statement

中国大陆的前端与独立开发者在浏览器中调用第三方公共 HTTP API 时，经常同时遇到两类障碍：浏览器阻止读取跨域响应，以及目标站点从本地网络出口无法稳定连接。any-proxy 提供一个部署在用户自选服务器上的 URL 前缀式 Relay，由该服务器代发 HTTP(S) 请求，并向浏览器返回可读取的 CORS 响应。

首版不再追求最初的“任何访问都可以通过这个服务发出去”。它只处理 HTTP(S)，不提供 TCP、UDP、SOCKS、HTTP CONNECT 或整机代理。

## What Makes This Cool

用户只需要把：

```text
https://api.example.com/data?city=shanghai
```

改为：

```text
https://proxy.example.com/https://api.example.com/data?city=shanghai
```

同一条浏览器请求就会从用户部署服务器的网络出口发出，并获得可由前端 JavaScript 读取的响应。项目的“哇”点不是发明新的代理协议，而是把自托管、流式转发、跨域处理和逐跳公网目标校验压缩到一个 Rust 二进制与一个容器中。

## Constraints

- 面向中国大陆的前端与独立开发者，首发形态是开源、自托管工具。
- 使用 Rust 实现，产物是单一服务进程与版本化 Docker 镜像。
- 默认公网匿名开放，不要求 Token，不校验调用方 Origin，不限制目标域名。
- 不实现按调用方统计的请求额度、流量预算或响应大小配额；仍设置进程级并发、URI/header 大小与超时边界，防止单个连接永久占用进程。
- 允许任意公共 HTTP(S) URL；服务必须拒绝本机、私网、链路本地、保留地址和云元数据地址。
- 浏览器端不能依赖长期秘密；任何放进 JavaScript 的目标 API 凭证都可能被用户看到。
- 服务只能承诺“从部署服务器的网络出口发起请求”，不能承诺任何网站在任何地区都可访问。
- 由于公网匿名且无硬额度，本设计不能被描述为生产安全方案，最终交付状态为 `DONE_WITH_CONCERNS`。

## Premises

1. 首版是 URL 前缀式 HTTP(S) CORS Relay，不支持任意协议或整机流量代理。
2. 默认监听公网，任何来源均可匿名访问任意公共 HTTP(S) URL。
3. 不设置 Token、Origin 白名单、目标域名白名单、按调用方请求额度、流量预算或响应大小配额。
4. 每次创建上游连接时，DNS 解析、公网地址校验与实际 dial 必须在同一个 Connector 内绑定；连接池只复用已经建立且 peer IP 已验证的同 authority 连接。
5. 进程级并发、URI/header 大小、连接、TLS 握手、首字节、重定向次数、双向空闲读取和关闭期限属于资源回收与协议正确性机制，不属于使用额度。
6. 请求体和响应体必须流式传输，不能因内容大小线性占用进程内存。
7. 当前目录为空且不是 Git 仓库，没有既有代码、CI 或部署流程可以复用。

## Landscape Findings

- [`cors-anywhere`](https://github.com/Rob--W/cors-anywhere) 已证明 URL 前缀与 CORS 响应模型可行，也提供 Origin 白名单、必需请求头和限流配置。
- 其[公开实例限制记录](https://github.com/Rob--W/cors-anywhere/issues/45)显示，公网开放代理会带来集中滥用、平台性能压力和服务条款冲突。
- 较新的小型实现仍主要增加私网地址拦截、响应大小限制、媒体禁用、API Key 或容器分发，没有改变核心请求模型。
- [OWASP SSRF 防护指南](https://cheatsheetseries.owasp.org/cheatsheets/Server_Side_Request_Forgery_Prevention_Cheat_Sheet.html)要求限制协议、验证 IPv4/IPv6、处理 DNS 变化，并在重定向后重新验证目标。

EUREKA：浏览器前端无法保守长期 API Key，因此“加一个 Token 就安全”与目标用户天然冲突。用户最终选择放弃调用方访问控制，而不是放弃动态公共目标；这让逐跳内网地址防护成为核心中唯一不可退让的安全边界。

## Approaches Considered

### Approach A: 加固并封装 `cors-anywhere`

Fork 或包装现有 Node.js 项目，保留兼容 API，补充 Docker、私网地址拦截、重定向校验和中文文档。工作量最小，但旧代理栈很难证明“校验的 IP 就是实际建立连接的 IP”，长期还要承担上游结构与依赖更新风险。

### Approach B: 单体 Go 流式 Relay

使用 Go `net/http`、自定义 `DialContext` 和 `CheckRedirect` 集中控制 DNS、连接与重定向。安全边界清晰、容器简单；用户随后明确要求 Rust，因此此路线不再推荐。

### Approach C: OpenResty/Nginx 加 Lua 策略层

由成熟代理引擎处理连接与流式转发，Lua 层校验动态目标。数据面成熟，但 DNS、实际连接 IP 和重定向策略分散在多个层，测试与故障定位更难。

### Approach D: Rust 单体流式 Relay

使用 Tokio + Axum 处理入站 HTTP，使用 Hyper + Hyper-Rustls 的自定义 Connector 处理出站请求。Connector 原子执行 DNS 解析、公网地址校验与实际 dial，并记录最终 peer IP；请求和响应 Body 逐帧转发。该路线保留 Approach B 的集中式边界，同时满足用户指定的 Rust 技术栈。Reqwest 高层客户端不作为首版安全内核，因为其 Client 级 resolver、连接池和错误阶段不利于证明请求级连接绑定。

## Recommended Approach

选择 Approach D：Rust 单体流式 Relay。

### 1. Runtime and module boundaries

建议保持单 crate、单二进制，避免为首版建立 workspace 或插件系统：

```text
src/
  main.rs          进程启动、配置、优雅关闭
  app.rs           Axum Router 与共享状态
  proxy.rs         入站请求到上游请求的编排
  target.rs        URL 解析、协议与端口校验
  resolver.rs      DNS 解析、公网 IP 校验、连接地址固定
  redirect.rs      手动重定向与逐跳复查
  headers.rs       请求/响应头清理与 CORS 响应
  error.rs         稳定错误码与 HTTP 映射
  telemetry.rs     无敏感信息的结构化日志与指标
```

公共模块和函数使用中文文档注释；复杂的 DNS、连接固定和重定向逻辑补充中文行内注释。

### 2. HTTP interface

- `GET|HEAD|POST|PUT|PATCH|DELETE /<absolute-http-or-https-url>`：核心代理入口。
- `GET /healthz`：只报告进程存活，不探测任意外部站点。
- `GET /`：返回简短用法、风险提示和示例，不提供网页控制台。
- 所有入站 `OPTIONS` 都是 CORS 预检，不转发目标 `OPTIONS`。`CONNECT`、`TRACE`、WebSocket Upgrade 和自定义 method 返回 `405 method_not_allowed`，首版不提供隧道或升级语义。

目标 URL 必须从 Hyper/Axum 的原始 `OriginalUri` 取得，不能使用已经按路由语义解码或归一化的 path：

1. 读取原始 `path_and_query` ASCII 表示。
2. 只剥离恰好一个前导 `/`；不折叠重复斜线，不提前 percent-decode，不重新拼接 query。
3. 把剩余字节作为一个完整绝对 URL 交给 `url::Url` 解析一次；解析后的 canonical host 作为 DNS、Host 和 TLS SNI 的唯一主机解释。
4. 目标中的 query 原样保留；URL fragment 从不会出现在 HTTP request-target 中，文档明确说明 `#fragment` 无法代理。
5. `/https%3A%2F%2Fexample.com` 不会被二次解码成目标；正确形式是 `/https://example.com`。签名 URL 的路径和 query percent-encoding 必须保持不变。

固定测试向量覆盖目标 query、重复斜线、尾点域名、IDNA、IPv6 literal、percent-encoding、非法 userinfo 和双重编码，确保策略层与连接层只存在一种 URL 解释。

### 3. CORS behavior

- 预检固定返回 `204 No Content`、空 Body、`Access-Control-Allow-Origin: *`，不启用 credentials。
- `Access-Control-Request-Method` 必须是首版六个允许 method 之一；请求 header 名按 RFC token 解析，总长度不超过 8 KiB，非法值返回带 CORS 的 `400 invalid_preflight`。
- `Access-Control-Allow-Methods` 固定为 `GET, HEAD, POST, PUT, PATCH, DELETE, OPTIONS`；`Access-Control-Allow-Headers` 回显通过校验的请求 header 名。
- 预检响应添加 `Vary: Access-Control-Request-Method, Access-Control-Request-Headers` 和 `Access-Control-Max-Age: 600`。
- 正常响应与所有代理生成的错误响应添加 `Access-Control-Allow-Origin: *`。
- `Access-Control-Expose-Headers` 固定为 `Content-Type, Content-Length, Content-Encoding, ETag, Last-Modified, Cache-Control, Expires, Location, Accept-Ranges, Content-Range, X-Proxy-Request-Id`。Success Criteria 只承诺读取被保留且显式暴露的 headers。
- 不转发浏览器 `Cookie`，不返回目标 `Set-Cookie`，避免制造错误的跨域会话语义。
- 允许转发目标 API 所需的 `Authorization`，但 README 必须提醒：凭证会经过自托管代理，且公开前端中的凭证对终端用户可见。

### 4. Target validation and connection pinning

目标安全路径必须按以下顺序执行；任何策略失败都在建立上游连接前返回稳定错误：

1. 使用标准 URL 解析器解析绝对 URL；只允许 `http` 与 `https`，拒绝 userinfo、空主机、非法端口和歧义编码。
2. 规范化 IP 表示后分类，包括 IPv4-mapped IPv6、IPv4 literal、尾点域名和 IDNA host。整数、八进制、十六进制等非标准 IPv4 文本若标准解析器不接受则直接拒绝，不能自行宽松解析。
3. 对 IP literal 直接分类；对域名在 Connector 内同时解析 A 与 AAAA。只要答案中包含一个非公网地址，就拒绝整个目标，不能从混合答案中挑选公网地址继续。
4. 使用 IANA IPv4/IPv6 特殊用途地址表维护显式拒绝 CIDR，默认拒绝未知或无法分类的地址。表来源、抓取日期与生成脚本进入仓库；CI 检测上游表变化但不自动修改策略。
5. 启动时枚举宿主所有网络接口地址并加入 deny set，每 60 秒刷新；额外支持 `DENY_CIDRS`。NAT hairpin 对应的宿主公网地址可能无法从容器内自动发现，部署文档要求通过 `DENY_CIDRS` 或出口防火墙补充，这是已知残余风险。
6. 自定义 Hyper Connector 在一次调用中完成 resolve、全量校验、选择 `SocketAddr` 与 `TcpStream::connect`，并记录 `peer_addr()`。测试注入 Resolver 与 Dialer，断言策略允许集合与实际 dial 地址完全一致。
7. 不读取 `HTTP_PROXY`、`HTTPS_PROXY` 或 `ALL_PROXY` 环境变量。TLS 证书校验与 SNI 使用原始规范化主机名，TCP 使用 Connector 已固定的 IP。
8. 上游首版只启用 HTTP/1.1，避免 HTTP/2 跨 authority 连接合并扩大池语义。连接池只按原始 authority 复用已经建立且 peer IP 已验证的连接；新连接必经 Connector。池中连接不会因 DNS 后续变化而改连其他地址。

首版允许任意显式公共端口 `1..=65535`，这是用户“任意公共 HTTP(S) URL”决定的直接结果，也意味着实例可被用于公共端口扫描。README、Constraints 和测试必须显著记录；不把端口白名单偷偷加入实现。

不能采用“先查 DNS 校验，再让默认客户端按域名重新查一次”的实现。两次解析之间可能变化，检查结果与真实连接会失去绑定。

### 5. Redirect policy

- 最多跟随 10 跳，维护 canonical URL 集合并拒绝重定向环；阻止 HTTPS 降级到 HTTP。
- 只对 `GET` 与 `HEAD` 自动跟随。301/302/307/308 保持 GET 或 HEAD；303 对 GET 保持 GET、对 HEAD 保持 HEAD。
- POST、PUT、PATCH、DELETE 收到任何 3xx 时直接把合法上游 3xx、Body 与经清理的 `Location` 返回调用方，不尝试重放已经消费的流式请求体。
- 相对 `Location` 按当前 URL 解析；缺失或非法 `Location` 的 3xx 原样返回，不继续跟随。
- 每一跳从 Target validation 第 1 步重新开始。跨 origin 时删除 `Authorization` 及未来标记为 sensitive 的业务 headers；Cookie 本来就不转发。

### 6. Streaming, pooling and lifecycle

- Axum 入站 Body 通过 `http-body-util` 直接桥接为 Hyper 流式 Body，不完整缓冲请求。
- Hyper 上游响应 Body 直接桥接为 Axum 流式 Body，不完整缓冲响应；Hyper 不执行内容自动解压，因此 `Content-Encoding` 与 `Content-Length` 可在未改写 Body 时原样保留。
- 客户端断开后取消上游请求；响应 Drop 时关闭上游 Body。
- 固定默认值：`MAX_CONCURRENT_REQUESTS=256`、`MAX_HTTP1_BUFFER_BYTES=65536`、`MAX_HEADERS_COUNT=100`、`MAX_URI_BYTES=16384`、`DNS_TIMEOUT=5s`、`CONNECT_TIMEOUT=10s`、`TLS_TIMEOUT=10s`、`UPSTREAM_HEADERS_TIMEOUT=30s`、`UPLOAD_IDLE_TIMEOUT=30s`、`UPSTREAM_BODY_IDLE_TIMEOUT=60s`、`POOL_IDLE_TIMEOUT=30s`、`SHUTDOWN_GRACE=30s`。HTTP/1 parser buffer 与 header count 在 Hyper server builder 层限制，URI 在进入目标解析前再次检查。这些环境变量可调但必须为正数，并设置文档化上限。
- 并发上限由 Tower 层在解析 Body 前获取 permit；慢下游最多占用一个 permit，因此总资源仍有上界。上传与上游下载按相邻 frame 的空闲时间终止，不配置总 Body 字节数或总请求时长。
- 优雅关闭先停止接收新连接，等待 30 秒后取消剩余请求。
- 首版禁用所有自动 retry，避免非幂等重放和错误阶段歧义。

### 7. Header and trailer policy

请求与响应都先解析 `Connection` header 的逗号分隔 token，并删除其中点名的所有 headers；随后删除固定 hop-by-hop 集合：`Connection`、`Keep-Alive`、`Proxy-Authenticate`、`Proxy-Authorization`、`TE`、`Trailer`、`Transfer-Encoding`、`Upgrade`。

请求侧额外移除 `Host`、`Forwarded`、`Via`、`X-Forwarded-*`、`Proxy-*`、`Cookie` 和代理自身控制头；由客户端根据目标 URL 重建 `Host`。首版不转发 request trailers。

响应侧额外移除 `Set-Cookie`、上游 CORS headers 和可能暴露代理内部信息的 headers，然后写入统一 CORS headers。首版不转发 response trailers。保留合法上游状态码、`Content-Type`、`Content-Length`、`Content-Encoding`、`ETag`、缓存控制和安全业务 headers；由于首版不解压或改写 Body，这些长度与编码字段保持一致。

### 8. Error contract

只有响应 headers 发出前发生的代理错误使用稳定 JSON 结构：

```json
{
  "error": {
    "code": "target_blocked",
    "message": "目标地址不允许访问",
    "request_id": "..."
  }
}
```

固定状态映射：

- `400 invalid_target`：URL、协议、userinfo 或端口格式非法。
- `403 target_blocked`：解析到非公网地址、HTTPS 降级或被安全规则拒绝。
- `405 method_not_allowed`：不支持的方法、CONNECT、TRACE 或 Upgrade。
- `502 dns_failed | connect_failed | upstream_failed`：DNS、TCP/TLS 或上游协议失败。首版不承诺把 TCP 与 TLS 错误稳定细分。
- `504 connect_timeout | upstream_timeout`：连接阶段或响应 headers 前超时。
- 错误响应不返回已解析的私网 IP、完整查询参数、请求头或上游响应体。

上游返回的任何合法 HTTP 4xx/5xx 都按 Relay 语义原样转发，不转换成代理 JSON 错误。响应 headers 一旦发给调用方，后续 Body frame 错误或 idle timeout 已无法改写状态码：此时中止流/连接，调用方看到截断 Body，日志与指标记录 `stream_aborted`，不能承诺返回 JSON。

### 9. Observability and privacy

结构化日志只记录 `request_id`、方法、目标 scheme、规范化 hostname、端口、最终状态、错误码、持续时间和流式字节计数。不得记录 URL query、userinfo、请求/响应 headers、Cookie、Authorization 或 Body；日志中的 hostname 也应支持关闭。

M1 只提供结构化日志与进程内原子计数；Prometheus exporter 延后到 M2。指标用于观察开放代理风险，但按用户决定不触发按调用方额度或自动停机。

### 10. Dependencies

首版依赖保持小而明确：

- `tokio`：异步 runtime 与信号处理。
- `axum`：入站路由与服务端集成。
- `hyper`（`client,http1`）与 `hyper-util`：出站 HTTP/1.1 客户端、连接池和自定义 Connector 接入。
- `hyper-rustls`：保持原始 hostname 的证书校验与 SNI，同时使用 Connector 已固定的 TCP peer。
- `http-body-util`、`futures-util`：Axum/Hyper Body 桥接与按 frame idle timeout。
- `tower`：全局并发上限、load shedding 和服务层错误映射。
- `url`：标准 URL 解析。
- `ipnet`：显式 CIDR 匹配，配合项目内维护的 IANA 特殊用途地址表。
- `if-addrs`：枚举宿主可见网络接口，补充本机地址 deny set。
- `serde` / `serde_json`：配置与稳定错误响应。
- `tracing` / `tracing-subscriber`：结构化日志。

连接池是 M0 的显式风险点：M0 默认关闭上游 idle connection reuse，每次请求都走一次“resolve → 全量校验 → 固定 SocketAddr → dial”。只有在 Hyper/`hyper-util` 的真实 API spike 证明连接池 key 同时绑定 scheme、authority 与已验证 peer 集合，并能通过 DNS 变化和并发测试后，M1 才允许按 authority 复用；否则 M1 继续关闭池复用，不以性能换取地址绑定不确定性。

直接 feature 在 M0 锁定：Tokio 使用 `rt-multi-thread,macros,net,signal,time`；Axum/Hyper 只启用 HTTP/1.1 与 Tokio 集成；Hyper-Rustls 使用系统/打包 CA 根，不启用危险证书跳过；所有自动内容解压均不存在于 Hyper 数据面。

不在首版引入 Reqwest、数据库、Redis、消息队列、账户系统、管理 UI、插件框架或 Prometheus exporter。实现时统一由 `Cargo.toml` 与 `Cargo.lock` 锁定版本，运行 `cargo deny` 检查许可证、重复依赖与安全公告；依赖章节是 M0/M1 的穷举清单，新增直接依赖必须回到设计评审。

### 11. Container and process hardening

- 多阶段构建，运行镜像只包含二进制、CA 证书和非 root 用户。
- 默认只写 stdout/stderr，不依赖可写文件系统。
- 支持 `SIGTERM` 优雅关闭：停止接收新请求，等待活跃流在配置时间内结束后退出。
- 服务内部监听 HTTP；生产 HTTPS 由部署者已有反向代理终止。仓库提供可选 Caddy Compose 示例，但核心镜像仍是单容器。
- README 必须在运行命令之前显示“公网匿名开放代理、无额度控制”的风险说明。

## Closed Decisions and Residual Concerns

1. 首版允许任意公共端口，不提供端口白名单。这会使实例具备公共端口扫描能力，是用户选择“任意公共 URL”的已接受风险。
2. 首版不支持 WebSocket、CONNECT、TRACE、自定义 method、request/response trailers 或目标 OPTIONS。
3. 核心服务只监听 HTTP，公网 HTTPS 由外部 Caddy/Nginx/云负载均衡器终止；仓库只提供可选 Caddy Compose 示例。
4. CORS 不使用 wildcard expose headers，而显式暴露一组常用安全响应头。
5. 容器无法可靠自动发现 NAT hairpin 对应的宿主公网地址；部署者必须通过 `DENY_CIDRS` 或出口防火墙补充。
6. 公网匿名、无按调用方额度的风险仍然没有技术消除，文档状态保持 `DONE_WITH_CONCERNS`。
7. 没有代理层总字节额度意味着长期大流量仍可能耗尽部署者带宽；部署文档只能建议使用云厂商账单告警、出口防火墙和实例级网络策略，不能把这些外部措施伪装成 any-proxy 的配额能力。

## Success Criteria

完成定义拆成三个独立里程碑，前一阶段未通过不得进入后一阶段。

### M0: Security connector spike

1. `Target`、`Resolver`、`AddressPolicy`、`Dialer` 与 `Connector` 都可注入测试替身；Connector 记录最终 dial 的 `SocketAddr`。
2. 危险 IP literal、IPv4-mapped IPv6、特殊用途地址、宿主接口地址、混合 A/AAAA 答案和 `DENY_CIDRS` 全部产生零次 dial。
3. 允许目标的实际 dial peer 必须属于同一次 Resolver 返回且完成全量校验的地址集合；并发同主机、DNS 答案变化和连接池新建/复用测试不得产生跨请求映射竞态。
4. GET/HEAD 重定向到私网、HTTPS 降级、无效 Location 和重定向环全部按固定策略终止；带 Body method 的 3xx 不跟随。
5. 默认 CI 使用仓库内可复现的本地 HTTP/HTTPS fixture：HTTPS 使用测试 CA 与固定证书，DNS/Resolver/Dialer 均可注入；测试断言 TLS SNI 和 Host 仍使用原始 hostname。另提供 `E2E_PUBLIC=1` 的非阻塞手动任务，只有运行环境明确允许公网访问时才测试真实公共目标，不能作为合并门槛。

### M1: Complete relay

1. 浏览器把公共 HTTP(S) API URL 加到代理前缀后，可以读取状态码、显式保留/暴露的响应头和流式响应体。
2. GET、HEAD、POST、PUT、PATCH、DELETE 在不完整缓冲的情况下完成转发；入站 OPTIONS 按固定模板完成 CORS 预检，其他 method 返回 405。
3. 传输至少 256 MiB 的生成式响应时，额外常驻内存保持在固定小窗口内，不随响应大小线性增长。
4. 客户端中途断开、上传/下载 idle timeout 和优雅关闭都能取消上游工作；headers 发出后的流错误表现为截断流并记录 `stream_aborted`。
5. 动态 `Connection` header 清理、无 trailers、无自动解压、上游 4xx/5xx 原样转发均有集成测试。
6. 日志与错误中不出现 URL query、Authorization、Cookie、headers 或 Body。
7. `docker compose up -d` 后健康检查通过，示例 URL 从部署服务器网络出口完成请求。

### M2: Release supply chain

1. Linux `amd64` 与 `arm64` 镜像由 CI 构建，版本标签、镜像摘要和源码标签可追溯。
2. GitHub Release 包含校验文件；SBOM、provenance、容器启动测试和 `cargo deny` 全部通过。
3. Prometheus exporter、可选 Caddy Compose 与公开运维文档完成，但不改变 M1 的代理语义。

## Distribution Plan

- GitHub Container Registry 发布 `ghcr.io/<owner>/any-proxy:<semver>`、主版本标签和不可变 commit SHA 标签。
- GitHub Releases 附带变更说明、镜像摘要、Linux 静态二进制压缩包和 SHA-256 校验文件。
- M1 前 GitHub Actions 只运行 `cargo fmt --check`、`cargo clippy -- -D warnings`、M0/M1 单元测试和集成测试。
- M2 才启用 tag 多架构镜像发布、最小容器启动测试、SBOM 与 provenance，避免安全核心未证明前扩张发布工作。
- `docker-compose.yml` 提供核心单容器示例；另提供可选 Caddy TLS 终止示例，但不把 Caddy 编入核心镜像。
- 首版不运营官方公共代理实例；README 示例使用部署者自己的域名。

## Next Steps

1. M0：初始化 Rust crate，只实现 URL 解析、地址策略、可注入 Resolver/Dialer、Hyper Connector 与重定向状态机；默认关闭上游连接池，不先写完整 Axum Relay。
2. M0：完成危险目标零 dial、允许集合等于 peer、并发 DNS 变化测试；默认无池模式下只验证连接不会被错误复用，只有连接池 API 也通过同一组测试时才评估开启复用，未通过则保持无池模式。
3. M1：接入 Axum、固定 method/CORS/header/error contract、Body 桥接、并发上限、取消传播和 idle timeout。
4. M1：完成 Docker、Compose、结构化日志、真实浏览器与远端服务器端到端验证。
5. M2：完成多架构发布、SBOM、provenance、Prometheus exporter、Caddy 示例和发布说明。

## The Assignment

只完成 M0，不初始化完整 Relay、Docker 或发布流水线：用可注入 Resolver/Dialer、恶意 URL、IPv4/IPv6、宿主接口、DNS 变化和重定向夹具证明危险目标产生零 dial，允许目标的实际 peer 严格属于同一次已验证地址集合，并完成仓库内可复现的本地 HTTP(S) fixture。公网 HTTP(S) 连接仅作为环境允许时的手动 `E2E_PUBLIC=1` 验证，不是 M0 合并门槛。只有 M0 证明“检查与连接绑定”后，才进入 M1。

## What I noticed about how you think

- 你一开始说“任何访问都可以通过这个服务来发出去”，随后在体验选择中选了 URL 前缀方案；这把范围从通用网络隧道收窄成了可以单独交付的 HTTP(S) 工具。
- 当风宝推荐目标白名单时，你选择了“默认允许任意公共 URL”；当风宝继续推荐硬预算时，你两次选择了“公网匿名且没有硬上限”。这些是明确的产品方向，文档也明确保留了对应风险。
- 在 Go 单体路线被推荐后，你直接说“我想使用rust来实现”。最终路线因此保留单体安全边界，但把技术栈改为 Rust，而不是把语言偏好当作次要细节略过。
